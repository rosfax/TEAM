# 🧩 Модель наследования и уточнения TEAMS Policy

---

## 1. Базовый принцип — “Конституция и муниципалитеты”

- **TEAMS Manifest** — это *основной нормативный акт* (уровень организации).  
  Он задаёт:
  - структуру PDCA;
  - роли и их контракт (RACI);
  - pipeline стадии и взаимосвязи;
  - категории изменений (A/B/C);
  - принципы HITL;
  - базовую политику безопасности и прозрачности.

- **Проектные policies** — это *локальные уточнения* (уровень бизнес-юнита).  
  Они не могут:
  - изменять или удалять базовые стадии PDCA,
  - подменять RACI,
  - ослаблять HITL-гейты или риск-классификацию.

  Но они могут:
  - добавлять детали к поведению агентов,
  - вводить новые типы артефактов,
  - уточнять UX-команды, нотацию или правила общения,
  - задавать доменные исключения (например, для финтеха или контентных проектов).

---

## 2. Иерархия политики (Policy Inheritance Model)

```
[ TEAMS Manifest (v1.1.1) ]
        ↓
[ Organization-level policies ]
        ↓
[ Project-level policies ]
        ↓
[ Agent-level customization (profiles/*.yaml) ]
```

🧠 Логика:
- **Manifest** — жёсткое основание (нельзя изменять pipeline, RACI, risk-классы).  
- **Org Policies** — системные нормы, действующие во всех проектах компании.  
- **Project Policies** — конкретизация в рамках контекста продукта.  
- **Agent Profiles** — стилистические и UX-настройки поведения в Slack.

---

## 3. Механика декларации наследования

Каждый проект хранит свой файл `team.lock.yaml`, где зафиксировано, от какой версии TEAMS и от каких политик он наследует:

```yaml
teams_version: 1.1.1
extends:
  - /org/policies/communication.human-layer.yaml
  - /org/policies/security.base.yaml
  - /org/policies/risk-control.yaml
overrides:
  - /projects/cryptoarb/policies/communication.crypto-layer.yaml
  - /projects/cryptoarb/policies/domain-specific.yaml
```

Таким образом:
- TEAMS Manifest задаёт непереопределяемые принципы.
- Любое “override” проверяется policy-checker’ом —  
  и если оно нарушает TEAMS-ограничение, merge блокируется.

---

## 4. Типы допустимых изменений в проектах

| Уровень | Пример изменений | Разрешённость |
|----------|------------------|----------------|
| **Organizational** | Настройка Slack-команд, UX стиля, шаблонов PDCA-артефактов | ✅ |
| **Project** | Добавление доменных ролей (например, “Trader”), уточнение сообщений | ✅ |
| **Policy-level** | Изменение логики HITL, удаление стадий | ❌ |
| **Manifest-level** | Попытка переопределить RACI или Risk Class | 🚫 запрещено |

---

## 5. Как проект расширяет “Human-Layer Policy”

Проект может дополнять **Communication Blueprint** собственным “надстроечным” policy-файлом, например:

`/projects/cryptoarb/policies/communication.crypto-layer.yaml`

```yaml
extends: /org/policies/communication.human-layer.yaml
version: 0.9.1
status: local-override

modifications:
  - agent_behavior:
      Olya:
        tone: "более аналитичный, без смайлов"
        allowed_commands:
          - /analyze
          - /compare
          - /summarize-thread
      Gosha:
        tone: "сухой, инженерный"
        response_style: "в код-блоках"
  - commands:
      add:
        - /simulate-latency <ms>
        - /api-benchmark <endpoint>
```

📌 При этом policy-checker TEAMS проверяет:
- не нарушена ли роль агента (Olya всё ещё Researcher?);
- не введено ли поведение вне её RACI;
- не ослаблены ли контрольные гейты или HITL.

---

## 6. Формула проверки легитимности политики

```
VALID_POLICY = (policy ⊆ allowed_scope_of_manifest)
AND (policy ∉ forbidden_domains)
AND (policy extends TEAMS Manifest)
```

Если нарушено → policy получает статус `invalid`  
и не подгружается в проект до утверждения через audit-loop.

---

## 7. Практический пример: разные проекты, один каркас

| Проект | Особенности | Policy Layer |
|---------|--------------|---------------|
| **CRYPTOARB** | Время критично → нужно больше autonomy у Builder | `/policies/communication.crypto-layer.yaml` |
| **CONTENTLAB** | Больше внимания к UX и Human QA | `/policies/communication.editorial-layer.yaml` |
| **ANALYTICA** | Фокус на исследовательских потоках | `/policies/communication.research-layer.yaml` |

У всех — один backbone (`communication.human-layer.yaml`),  
но каждый слой уточняет поведение агентов, стиль и команды.

---

## 8. Проверка и аудит изменений

Любое обновление или добавление policy:
1. проходит TEAMS **policy-linter**;
2. получает метку `Class A/B/C`;
3. при необходимости идёт через **HITL audit-loop** (особенно если Class C);
4. результат фиксируется в `audit_report.yaml`.

---

## 9. Резюме архитектуры

> 🧭 **TEAMS Manifest** = Конституция.  
> 📜 **Organization Policies** = федеральные законы.  
> 🏗 **Project Policies** = локальные постановления.  
> 🎭 **Agent Profiles** = индивидуальные привычки сотрудников.  

Все действуют в одной правовой вселенной,  
где порядок и человечность сосуществуют.
