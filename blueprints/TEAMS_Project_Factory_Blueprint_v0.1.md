# 🏗 TEAMS Project Factory — Blueprint v0.1

## 🎯 Цель
Создать **автоматизированный конвейер** для инициализации новых проектов (CRYPTOARB, DOTCLUB и т.д.),  
чтобы из одной команды или Slack-команды `/init-project`  
фабрика полностью разворачивала:
- Slack workspace  
- GitHub репозиторий  
- базовые шаблоны TEAMS (lock, policies, templates)  
- CI/CD скелет  
- Airflow или execution-plane инстанс  

И всё это — **по канону TEAMS Manifest v1.1.1**,  
без нарушения PDCA и RACI.

---

## 🧭 1. Позиция фабрики в TEAMS-архитектуре

| Уровень | Роль фабрики |
|----------|---------------|
| **TEAMS Core (Manifest)** | Фабрика реализует стадию **Initiate → Plan** для новых проектов. |
| **Projects** | Создаёт локальные TEAMS-инстанции (`team.lock.yaml`, `policies.local.yaml`, templates). |
| **Integration Plane** | Подключает Slack, GitHub, Airflow через API. |
| **Execution Plane** | Разворачивает инфраструктуру (репозиторий, CI, Airflow). |

📌 Фабрика **не заменяет TEAMS**,  
она исполняет его начальные стадии от имени Project Initiator.

---

## 🧩 2. Концептуальная схема

```
Slack (/init-project)
        ↓
TEAMS Factory API
        ↓
Project Template Engine
        ↓
GitHub API + Slack API + Vault API + Airflow API
        ↓
New Project (fully initialized)
```

---

## ⚙️ 3. Основные модули фабрики

| Модуль | Назначение | Артефакты / Выход |
|---------|-------------|--------------------|
| **Factory CLI / Slack Bot** | Точка входа (`/init-project`) | Формирует `project_init.yaml` |
| **Template Engine** | Разворачивает шаблон TEAMS | `team.lock.yaml`, `policies.local.yaml`, `README.md` |
| **GitHub Provisioner** | Создаёт репо, загружает шаблон, настраивает CI | `rosfax/<project>` |
| **Slack Provisioner** | Создаёт workspace + базовые каналы | `#tasks`, `#ops`, `#rfc` |
| **Secrets Manager** | Заливает API-токены и ключи | GitHub Secrets / Vault entries |
| **Orchestrator Hook** | Поднимает Airflow или регистрирует DAGs | `pipelines/*.py` или `docker-compose.yml` |
| **Tracer / Audit Agent** | Логирует trace_id и связь с фабрикой | `factory_run_report.yaml` |

---

## 🪄 4. Минимальный сценарий работы

**Инициатор (в Slack):**
```
/init-project CRYPTOARB --template=default --owner=rosfax
```

**Factory Bot отвечает:**
> Инициирую проект CRYPTOARB 🏗  
> Создаю репозиторий, workspace и базовые артефакты TEAMS v1.1.1...  
> ⏳

**Шаги фабрики (в логике PDCA):**

| TEAMS стадия | Действие | Артефакт |
|---------------|-----------|-----------|
| **Initiate** | создаётся `project_init.yaml` | описание проекта |
| **Think** | выбирается шаблон `default` | путь к шаблону |
| **Plan** | генерируется структура | `team.lock.yaml`, policies |
| **Build** | создаются GitHub + Slack | репо, каналы |
| **Ship** | подтверждение успешного деплоя | `factory_run_report.yaml` |

---

## 🧠 5. Минимальный набор файлов, создаваемых фабрикой

```
/projects/<name>/
  ├── team.lock.yaml
  ├── policies.local.yaml
  ├── tasks/
  ├── pipelines/
  ├── .github/
  │    ├── ISSUE_TEMPLATE/
  │    ├── workflows/
  ├── README.md
  └── init_report.yaml
```

+ Slack workspace:  
  `#tasks`, `#ops`, `#rfc`, `#build`  
+ Secrets: `GITHUB_TOKEN`, `SLACK_APP_TOKEN`, `AIRFLOW_URL`, `FACTORY_TRACE_ID`

---

## 🔐 6. API и интеграции

| Сервис | API / SDK | Используемые операции |
|--------|------------|------------------------|
| **GitHub** | REST v3 | `POST /orgs/{org}/repos`, `PUT /repos/{org}/{repo}/contents` |
| **Slack** | SCIM / Admin / Web API | `admin.teams.create`, `conversations.create`, `users.invite` |
| **Vault / Secrets** | HashiCorp Vault API | `POST /v1/secret/data` |
| **Airflow** | REST API v2 | `POST /dags`, `GET /health` |
| **TEAMS Core** | internal YAML API | чтение шаблонов, версия Manifest |

---

## 🧱 7. Артефакты TEAMS, создаваемые фабрикой

| Артефакт | Назначение |
|-----------|-------------|
| `project_init.yaml` | метаданные проекта (инициатор, дата, шаблон) |
| `team.lock.yaml` | зафиксированная версия TEAMS |
| `policies.local.yaml` | уточнения поведения |
| `factory_run_report.yaml` | журнал действий фабрики |
| `init_trace_id` | общий trace для TEAMS backend |

---

## 🧩 8. Этапность внедрения

| Этап | Фокус | Реализация |
|------|--------|-------------|
| **I. Архитектура (сейчас)** | зафиксировать модули и API | Blueprint (этот документ) |
| **II. Manual Factory (CRYPTOARB)** | ручное выполнение шагов фабрики | через чеклист |
| **III. Scripted Factory (DOTCLUB)** | bash/python automation MVP | GitHub + Slack API |
| **IV. Full API Factory** | Slack Bot + backend orchestration | полноценный `/init-project` |

---

## 🧩 9. Логика интеграции с TEAMS Manifest

- фабрика **исполняет стадии Initiate → Plan**  
- при этом создаёт полный PDCA-контур для проекта (Think/Build/Ship)
- результат фабрики — **проект, который уже “жизнеспособен”** в TEAMS Loop  
- сам процесс фабрики фиксируется в `factory_run_report.yaml`  
  → это артефакт класса **A (Integration)** по Manifest v1.1.1

---

## 🧭 10. Контрольная точка готовности (Definition of Done)

Фабрика считается работоспособной, если:

✅ `rosfax/<new_project>` создаётся полностью через API  
✅ Slack workspace готов и интегрирован с GitHub  
✅ В репозитории есть `team.lock.yaml` и базовые templates  
✅ Можно написать в Slack `/create-task` → и дойти до первого PR  
✅ Все шаги трассируются через `factory_run_report.yaml`
